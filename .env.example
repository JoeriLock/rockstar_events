SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True

# Set to True to send a verification email on registration; False = manual admin approval
REQUIRE_EMAIL_VERIFICATION=False

# Email — leave EMAIL_BACKEND unset (or use console) for local dev
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.office365.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=you@teamrockstars.nl
# EMAIL_HOST_PASSWORD=your-password
# DEFAULT_FROM_EMAIL=noreply@teamrockstars.nl
